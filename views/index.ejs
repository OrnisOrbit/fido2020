<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <title>Fido2020</title>
    <link rel="stylesheet" href="styles.css">
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.4.1/css/bootstrap.min.css"
        integrity="sha384-Vkoo8x4CGsO3+Hhxv8T/Q5PaXtkKtu6ug5TOeNV6gBiFeWPGFN9MuhOf23Q9Ifjh" crossorigin="anonymous">
    <script src="https://cdn.jsdelivr.net/npm/popper.js@1.16.0/dist/umd/popper.min.js"
        integrity="sha384-Q6E9RHvbIyZFJoft+2mJbHaEWldlvI9IOYy5n3zV9zzTtmI3UksdQRVvoxMfooAo" crossorigin="anonymous">
    </script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.4.1/js/bootstrap.min.js"
        integrity="sha384-wfSDF2E50Y2D1uUdj0O3uMBJnjuUD4Ih7YwaYd1iqfktj0Uod8GCExl3Og8ifwB6" crossorigin="anonymous">
    </script>
    <script src="https://code.jquery.com/jquery-3.4.1.min.js"
        integrity="sha256-CSXorXvZcTkaix6Yvo6HppcZGetbYMGWSFlBw8HfCJo=" crossorigin="anonymous"></script>
    <script>
       $( document ).ready(function() {
    console.log( "ready!" );
});
    </script>
    </head>

<body>
    <header>
        <nav class="navbar navbar-dark bg-primary">
            <a class="navbar-brand" href="/">
                <h1>Fido2020</h1>
            </a>
            <div class="btn-group">
                <% if (session.authorized) { %>
                <form action="/logout" method="POST">
                    <input class="btn btn-outline-light" type="submit" id="logout"
                        value="Logout @(<%= session.login %>)">
                </form>
                <% } else { %>
                <a class="btn btn-outline-light" href="/login">Login</a></li>
                <a class="btn btn-outline-light" href="/register">Register</a></li>
                <% } %>
            </div>
            </div>
    </header>
    <main>
        <div class="container">
            <% if (session.error) { %>
            <div class="row justify-content-md-center">
                <div class="col-md-4">
                    <div class="alert alert-danger">
                        <%= session.error %>
                        <% delete session.error; %>
                    </div>
                </div>
            </div>
            <% } %>
            <% if (session.authorized) { %>
            <div class="row justify-content-md-center">
                <div class="col-md-6">
                    <div class="px-5 m-2">
                        <form action="/message/create" method="POST">
                            <label class="text-primary"><strong>What’s on your mind?</strong></label>
                            <div class="input-group mb-3">
                                <input class="form-control text-white border-primary bg-primary" type="text"
                                    name="content" autocomplete="off">
                                <div class="input-group-append">
                                    <input class="btn btn-primary" type="submit" value="Send">
                                </div>
                            </div>
                        </form>
                    </div>
                </div>
            </div>
            <% } %>
            <% if (messages.length === 0) { %>
            <div class="row justify-content-md-center">
                <div class="col-md-4">
                    <div class="alert alert-primary">No messages...</div>
                </div>
            </div>
            <% } else { %>
            <div class="row mt-3">
                <% for (const message of messages) { %>
                <div class="col-md-4">
                    <% if (session.userID === message.user.id) { %>
                    <div class="card p-3 m-2 text-white bg-primary">
                        <% } else { %>
                        <div class="card p-3 m-2 text-white bg-secondary">
                            <% } %>
                            <h5 class="card-title">@<%= message.user.login %></h5>
                            <div class="card-text"><%= message.content %></div>
                            <time
                                class="card-subtitle"><small><%= moment(message.createdAt).format('LLL') %></small></time>
                            <% if (session.administrator || session.userID === message.user.id) { %>
                            <div class="clearfix">
                                <form class="ml-1 float-right" action="/message/<%= message.id %>/edit" method="GET">
                                    <input class="btn btn-sm btn-outline-light" type="submit" value="Edit">
                                </form>
                                <form class="mr-1 float-right" action="/message/<%= message.id %>/delete" method="POST">
                                    <input class="btn btn-sm btn-outline-light" type="submit" value="Delete">
                                </form>
                            </div>
                            <% } %>
                        </div>
                    </div>
                    <% } %>
                </div>
                <% } %>
            </div>
    </main>
    <footer>
        <nav class="navbar navbar-light bg-light">
            <p class="mx-auto mt-3">
                &copy; 2020
            </p>
            </div>
    </footer>
</body>

</html>